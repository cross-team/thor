import React from 'react';
import { storiesOf } from '@storybook/react';
import Button from '../../page/Checkboxes';

storiesOf('Checkboxes', module).add('with text', () => <Button>Hello Button</Button>);
